{
  "name": "@gravital/sound-web",
  "version": "0.1.0-alpha.1",
  "description": "Gravital Sound — SDK para navegadores (WebSocket transport + WASM).",
  "main": "js/index.ts",
  "types": "js/index.ts",
  "files": [
    "js/",
    "pkg/"
  ],
  "scripts": {
    "build": "wasm-pack build --target web --out-dir pkg --release",
    "build:dev": "wasm-pack build --target web --out-dir pkg --dev",
    "tsc": "tsc --noEmit"
  },
  "author": "Angel Nereira <angelnereira15@gmail.com>",
  "license": "MIT OR Apache-2.0",
  "devDependencies": {
    "typescript": "^5.5.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/angelnereira/gravital-sound.git",
    "directory": "sdks/web"
  }
}
